import { Meta } from '@storybook/addon-docs'

<Meta title="Foundations/Layout/Grid/Changelog" />

# Grid — Changelog

- **2026-06-18 (interactive Playground)** — Added a **Playground** story (Elastic-EUI style) using the
  real `MRow`/`MCol`: pick a **column size** (the row fills with `12/size` columns), toggle the **16px
  gutter**, and switch on **Auto-size** to see the canonical "filling field + content-width action
  buttons" row (auto-size belongs *beside* a filling field, not appended to a full row) — all live.
- **2026-06-17 (Usage page)** — Added the **Usage** page: when to use the grid, the size table, gutter
  rules, best practices, and links to FormItem + Page templates.
- **2026-06-17 (created)** — Added the **Grid** page — the 12-col `MRow`/`MCol` system with Columns,
  Form layout (gutter 16) and Auto-size stories, plus the machine spec `layout/grid.json` (sizes with
  in-product usage counts). Verified light + dark, deployed.
