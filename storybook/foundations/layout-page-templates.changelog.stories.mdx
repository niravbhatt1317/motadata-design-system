import { Meta } from '@storybook/addon-docs'

<Meta title="Foundations/Layout/Page templates/Changelog" />

# Page templates — Changelog

- **2026-06-18 (folded in the product-sweep findings)** — Added a 7th archetype — the **Graph / Canvas
  view** (Topology: hierarchy tree + Cytoscape canvas + minimap + zoom controls + severity-coloured
  nodes) — with its own wireframe and components table. Added an "Other archetypes & variants" Usage
  section (utility-tool, system/status, live-tail, list⇄dashboard view-toggle, detail-as-template-swapper,
  two wizard shapes) and a Graph/Canvas row in "Choosing a template".
- **2026-06-17 (Usage page)** — Added a rich **Usage** page: each template (List / Form / Detail /
  Dashboard / Explorer / Wizard) now lists **where it is used in the product** (modules / flows / pages),
  **when to use it + best practices**, and a **components-by-region table** with **links** to each
  component's stories. Added a "Choosing a template" decision table.
- **2026-06-17 (created)** — Added the **Page templates** section — six annotated wireframes built from
  the composition recipes (`recipes.json`), each naming the component used per region. Verified in light
  and dark, deployed.
