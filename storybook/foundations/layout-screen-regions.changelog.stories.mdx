import { Meta } from '@storybook/addon-docs'

<Meta title="Foundations/Layout/Screen regions/Changelog" />

# Screen regions — Changelog

- **2026-06-18 (interactive Playground)** — Added a **Playground** story (Elastic-EUI style): toggle the
  optional **Toolbar** and **Footer** regions, and use a **Body** selector to morph the content panel
  through all **6 content layouts** live.
- **2026-06-18 (folded in the product-sweep findings)** — Content layouts **4 → 6**: added **three-pane**
  (metric/apm explorer: saved-views rail + picker + chart) and **chart-over-grid** (trap viewer / log
  dashboards). Usage gained an **explorer control bar** + **time-range** region variant and a
  **left-panel variants** note (tree / saved-views / metric-picker / vertical-filter).
- **2026-06-17 (leader-line redesign + bigger frames)** — Rebuilt **Regions** as one page frame with
  right-side leader-line callouts (primary accent = required regions), and **Content layouts** as
  enlarged variant frames (**420×300**, same size as Panel behaviours) with labelled chips.
- **2026-06-17 (Usage page)** — Added the **Usage** page: a **regions → components** table (with links),
  a "content layouts — when to use" table, and best practices.
- **2026-06-17 (created)** — Added the **Screen regions** page — the page-header → toolbar → body →
  footer region stack and the 4 content layouts (single / two-pane / master-detail / dashboard), with
  in-product usage counts. Verified light + dark, deployed.
