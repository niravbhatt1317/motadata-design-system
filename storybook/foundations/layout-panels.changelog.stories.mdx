import { Meta } from '@storybook/addon-docs'

<Meta title="Foundations/Layout/Panel behaviours/Changelog" />

# Panel behaviours — Changelog

- **2026-06-18 (folded in the product-sweep findings)** — Grew the diagram from 5 → **10 surfaces**:
  added **Modal**, **Expandable rows**, **Bulk-action bar**, **Popover**, and **Full-screen / OmniBox**.
  Corrected the metas (**Resizable** = `splitpanes`, ~3 real drag-splits; **Affix** = CSS/JS sticky, no
  widget) and noted table column-resize is a grid feature, not a panel. Usage table + best practices
  rewritten; counts cited from `foundation/panel-behaviours.md`.
- **2026-06-17 (leader-line redesign + bigger frames)** — Switched to **Atlassian-style leader-line
  callouts** — each behaviour is a variant frame with a labelled chip + meta — and enlarged the frames to
  **420×300** in a 2-column grid.
- **2026-06-17 (Usage page)** — Added the **Usage** page: a "which behaviour to use" table (with
  component links + in-product usage counts) and best practices.
- **2026-06-17 (created)** — Added the **Panel behaviours** page — Drawer / Collapsible / Resizable /
  Affix / Dashboard tiles, each a mini wireframe with usage. Verified light + dark, deployed.
