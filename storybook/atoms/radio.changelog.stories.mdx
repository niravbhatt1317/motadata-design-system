import { Meta } from '@storybook/addon-docs'

<Meta title="Atoms/Radio/Changelog" />

# Radio — Changelog

## 2026-06-07

- Added to the design system — authored to the **decision-grade Usage** standard from the
  start (decision flow + per-type use cases, mirrored in `registry/radio.json`).
- Deep-dive of `RadioGroup.vue` / `Radio.vue`: used **225× / 136 files**; standalone `MRadio`
  is **0×** (radios always go through the group with `:options`); **`as-button`** segmented
  control used **255×**.
- Stories: Radio list · Segmented control · Disabled · Sizes. Verified list + segmented in
  light and dark (selected fill navy `#111c2c` → light `#e3e8f2`).
- Findings: **F1** (standalone `MRadio` + `variant`/`type` unused), **F2** (`buttonStyle`
  outline unused), **F3** (no focus ring → SF-001).
- Closes the **Button family's "segmented control" relative** (it's `MRadioGroup as-button`).
